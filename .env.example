# ============================================
# Telegram Bot Configuration
# ============================================

# Telegram Bot API token (from @BotFather)
BOT_TOKEN=your_bot_token_here

# Telegram MTProto credentials (from https://my.telegram.org/apps)
API_ID=12345678
API_HASH=your_api_hash_here

# ============================================
# Security
# ============================================

# Session encryption key
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SESSION_ENCRYPTION_KEY=your_32_byte_fernet_key_here

# ============================================
# Database
# ============================================

# PostgreSQL password (for docker-compose.yml with PostgreSQL)
# Generate secure password: openssl rand -base64 32
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Database URL (set automatically in docker-compose.yml)
# For local development without Docker:
# DATABASE_URL=sqlite+aiosqlite:///./data/bot.db
# For production with PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/tg_forward_bot

# ============================================
# Logging
# ============================================

LOG_LEVEL=INFO
LOG_FORMAT=json

# ============================================
# Forwarding Settings
# ============================================

# Max file size in MB for forwarding to DM (larger files will be sent as link only)
DM_MAX_MEDIA_SIZE_MB=20

# ============================================
# Keyword Filtering
# ============================================

# Comma-separated list of trigger words/phrases to filter messages
# Leave empty to disable filtering and forward all messages
# Example: block ads and spam
# FILTER_KEYWORDS_RAW=реклама,#реклама,ИП,ИНН,ОГРН,ERID,купить,скидка,курс

# Filter mode:
# - blacklist: skip messages containing keywords (default)
# - whitelist: only forward messages containing keywords
# FILTER_MODE=blacklist

# Case sensitivity for keyword matching (true/false)
# FILTER_CASE_SENSITIVE=false

# ============================================
# Optional: Redis (for future scaling)
# ============================================

# REDIS_URL=redis://localhost:6379/0
